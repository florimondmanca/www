{
  "name": "blog",
  "version": "1.0.0",
  "author": "Florimond Manca",
  "license": "MIT",
  "scripts": {
    "_esbuild": "esbuild ./server/web/styles/styles.css --bundle --outdir=./server/web/static/css --format=esm",
    "build": "npm run _esbuild -- --minify",
    "watch": "npm run _esbuild -- --watch=forever"
  },
  "devDependencies": {
    "esbuild": "^0.19.9"
  },
  "engines": {
    "node": "18.x"
  }
}
