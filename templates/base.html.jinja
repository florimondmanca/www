<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <meta
      name="description"
      content="Open Source Developer & Idealist On A Journey"
    />
    <meta name="author" content="Florimond Manca" />

    <title>Florimond Manca</title>

    <!-- Bootstrap core CSS -->
    <link
      href="{{ url_for('static', path='/css/bootstrap.min.css') }}"
      rel="stylesheet"
    />

    <!-- Iconic icons CSS -->
    <link
      href="{{ url_for('static', path='/css/open-iconic-bootstrap.min.css') }}"
      rel="stylesheet"
    />

    <!-- Custom styles for this template -->
    <link
      href="{{ url_for('static', path='/css/jumbotron.css') }}"
      rel="stylesheet"
    />
  </head>

  <body>
    <nav class="navbar navbar-dark fixed-top bg-dark">
      <div class="container">
        <a class="navbar-brand" href="{{ url_for('home') }}">
          Florimond Manca
        </a>
        <ul class="navbar-nav mr-auto"></ul>
      </div>
    </nav>
    {% block content %}{% endblock %}

    <!-- Bootstrap core JavaScript -->
    <!-- ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="{{ url_for('static', path='/js/jquery-3.3.1.slim.min.js') }}"></script>
    <script src="{{ url_for('static', path='/js/popper.min.js') }}"></script>
    <script src="{{ url_for('static', path='/js/bootstrap.min.js') }}"></script>
    {% block tail %}{% endblock %}
  </body>
</html>
